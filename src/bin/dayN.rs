use std::fs::read_to_string;

fn main() {
    let input = read_to_string("inputs/day3_test").unwrap().to_string();
    p1(&input);
    p2(&input);
}

fn p1(input: &String) {
    println!("{}", input);
}

fn p2(input: &String) {
    println!("{}", input);
}
